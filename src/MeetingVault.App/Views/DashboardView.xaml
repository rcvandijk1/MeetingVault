<UserControl x:Class="MeetingVault.App.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:MeetingVault.App.Converters">
    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Dashboard" Style="{StaticResource HeaderText}" Grid.Row="0"/>

        <Border Grid.Row="1" Style="{StaticResource Card}" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Detection" Style="{StaticResource MutedText}" FontSize="11"/>
                    <TextBlock Text="{Binding DetectionStatus}" FontSize="16" FontWeight="SemiBold" Margin="0,4,0,4"/>
                    <TextBlock Text="{Binding DetectionReason}" Style="{StaticResource MutedText}" FontSize="11"/>
                </StackPanel>
                <StackPanel Grid.Column="1" HorizontalAlignment="Right">
                    <TextBlock Text="Platform" Style="{StaticResource MutedText}" FontSize="11" HorizontalAlignment="Right"/>
                    <TextBlock Text="{Binding DetectedPlatform}" FontSize="22" FontWeight="Bold" HorizontalAlignment="Right"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource Card}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Recording" Style="{StaticResource MutedText}" FontSize="11"/>
                        <TextBlock Text="{Binding Duration}" FontSize="40" FontWeight="Bold" FontFamily="Consolas"/>
                        <TextBlock Text="{Binding StatusMessage}" Style="{StaticResource MutedText}" Margin="0,4,0,0"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                        <Button Content="Start Recording"
                                Style="{StaticResource PrimaryButton}"
                                Command="{Binding StartRecordingCommand}"
                                Visibility="{Binding IsRecording, Converter={StaticResource BoolToVis}, ConverterParameter=invert}"/>
                        <Button Content="Stop Recording"
                                Style="{StaticResource DangerButton}"
                                Command="{Binding StopRecordingCommand}"
                                Visibility="{Binding IsRecording, Converter={StaticResource BoolToVis}}"/>
                    </StackPanel>
                </Grid>

                <Grid Grid.Row="1" Margin="0,16,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="0" Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Microphone" Grid.Column="0" VerticalAlignment="Center"/>
                        <ProgressBar Grid.Column="1" Height="10" Minimum="0" Maximum="1"
                                     Value="{Binding MicLevel, Mode=OneWay}" Foreground="#4F8CFF"/>
                    </Grid>
                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="System audio" Grid.Column="0" VerticalAlignment="Center"/>
                        <ProgressBar Grid.Column="1" Height="10" Minimum="0" Maximum="1"
                                     Value="{Binding LoopbackLevel, Mode=OneWay}" Foreground="#7BD389"/>
                    </Grid>
                </Grid>

                <StackPanel Grid.Row="2" Margin="0,16,0,0">
                    <TextBlock Text="Last meeting" Style="{StaticResource MutedText}" FontSize="11"/>
                    <TextBlock Text="{Binding LastMeetingSummary}" FontSize="13" Margin="0,4,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,16,0,0">
            <Button Content="Open Meetings Folder" Command="{Binding OpenMeetingsFolderCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
