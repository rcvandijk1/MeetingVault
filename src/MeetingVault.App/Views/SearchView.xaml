<UserControl x:Class="MeetingVault.App.Views.SearchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Search Transcripts" Style="{StaticResource HeaderText}" Grid.Row="0"/>

        <Grid Grid.Row="1" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" Text="{Binding Query, UpdateSourceTrigger=PropertyChanged}"
                     FontSize="14" Padding="8,6">
                <TextBox.InputBindings>
                    <KeyBinding Key="Enter" Command="{Binding RunSearchCommand}"/>
                </TextBox.InputBindings>
            </TextBox>
            <Button Grid.Column="1" Content="Search" Style="{StaticResource PrimaryButton}"
                    Command="{Binding RunSearchCommand}" Margin="8,0,0,0"/>
        </Grid>

        <TextBlock Grid.Row="2" Text="{Binding StatusText}" Style="{StaticResource MutedText}" Margin="0,0,0,12"/>

        <Border Grid.Row="3" Style="{StaticResource Card}">
            <ListBox ItemsSource="{Binding Results}" BorderThickness="0" Background="Transparent"
                     HorizontalContentAlignment="Stretch">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#1A1C22" BorderBrush="{StaticResource BorderBrush}"
                                BorderThickness="1" CornerRadius="6" Margin="0,0,0,8" Padding="12">
                            <StackPanel>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="{Binding Subject}" FontWeight="SemiBold" FontSize="14"/>
                                        <TextBlock>
                                            <Run Text="{Binding StartTime, StringFormat='{}{0:yyyy-MM-dd HH:mm}'}"/>
                                            <Run Text=" — "/>
                                            <Run Text="{Binding Platform}"/>
                                        </TextBlock>
                                    </StackPanel>
                                    <Button Grid.Column="1" Content="Open folder"
                                            Command="{Binding DataContext.OpenMeetingCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"/>
                                </Grid>
                                <TextBlock Margin="0,8,0,0" TextWrapping="Wrap">
                                    <Run FontWeight="SemiBold" Text="{Binding SegmentStart, StringFormat='{}[{0:hh\\:mm\\:ss}]'}"/>
                                    <Run Text=" "/>
                                    <Run Text="{Binding SpeakerName}" Foreground="#9AA0AC"/>
                                    <Run Text=": "/>
                                    <Run Text="{Binding Snippet}"/>
                                </TextBlock>
                                <TextBlock Style="{StaticResource MutedText}" FontSize="11" Margin="0,4,0,0">
                                    <Run Text="{Binding MatchCount}"/>
                                    <Run Text=" matching segment(s) in this meeting"/>
                                </TextBlock>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>
    </Grid>
</UserControl>
